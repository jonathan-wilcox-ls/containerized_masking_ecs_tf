# Local .terraform directories
.terraform/

# .tfstate files (state should be managed in a remote backend)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# (e.g., passwords, private keys). These should be managed securely elsewhere.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local development and testing
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore plan files saved before applying (e.g. terraform plan -out=tfplan)
*tfplan*

# Optional: ignore graph output files
# *.dot
